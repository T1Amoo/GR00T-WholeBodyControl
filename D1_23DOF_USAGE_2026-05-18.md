# D1 路径 23DoF 微调 · 使用说明

> 目标：把官方 29DoF SONIC 模型微调成"输出 29 维但其中 6 维永远 ≈ 0"的策略，可直接部署到 23DoF G1 真机，**不动 deploy / ONNX / C++**。
>
> 关联文档：
> - 设计 spec: [`D1_23DOF_DESIGN_2026-05-15.md`](D1_23DOF_DESIGN_2026-05-15.md)
> - 实施计划: [`D1_23DOF_PLAN_2026-05-15.md`](D1_23DOF_PLAN_2026-05-15.md)
> - 上游评估: [`23DOF_EVALUATION_2026-05-14.md`](23DOF_EVALUATION_2026-05-14.md)
>
> 本文按"一台干净 Linux 机器 → 跑出 Stage 0 baseline + Stage 1 D1 改动 smoke test"的顺序写，没接触过本项目也能照做。

---

## 一、安装

### 1.1 前置

| 项 | 要求 | 备注 |
|---|---|---|
| OS | Linux | 验证于 Ubuntu 24.04 / 内核 6.8 |
| Python | 3.11 | IsaacLab 2.3.2 强约束 |
| GPU | ≥ 8 GB VRAM | 本地验证用 RTX 4060 Laptop 8 GB |
| Conda | Miniconda / Anaconda | 用于隔离 `groot_wbc` env |
| 磁盘 | ≥ 100 GB 空闲 | IsaacSim ≈ 30 GB；motion_lib 数据集三份各 ≈ 8 GB |

### 1.2 IsaacLab v2.3.2

```bash
cd /media/woan/84a38787-1d4e-4ba7-892e-d1d90a009a8c/lgy
git clone --branch v2.3.2 https://github.com/isaac-sim/IsaacLab.git IsaacLab-v2.3.2
cd IsaacLab-v2.3.2
cat VERSION   # 应输出 2.3.2
```

> **不要动**已有的 `IsaacLab-v2.1.0/`（实际 VERSION 是 2.0.2，归 Beyondmimic 用）。两份并列存在，不冲突。

### 1.3 创建 `groot_wbc` conda env

```bash
conda create -n groot_wbc python=3.11 -y
conda activate groot_wbc
pip install --upgrade pip
```

**安装坑位**（参考 `memory:isaaclab_v232_install_pitfalls`）：

- `flatdict` 需要 `setuptools<81`
- IsaacSim EULA：`export OMNI_KIT_ACCEPT_EULA=YES`
- pip resolver warnings 可以忽略

### 1.4 安装 IsaacSim + IsaacLab

```bash
pip install 'isaacsim[all,extscache]==5.0.0' --extra-index-url https://pypi.nvidia.com
cd /media/woan/84a38787-1d4e-4ba7-892e-d1d90a009a8c/lgy/IsaacLab-v2.3.2
./isaaclab.sh -i

# 验证
python -c "import isaaclab; print(isaaclab.__version__)"   # 2.3.2
python -c "from isaacsim import SimulationApp; print('isaacsim OK')"
```

### 1.5 安装 gear_sonic 训练依赖

```bash
cd /media/woan/84a38787-1d4e-4ba7-892e-d1d90a009a8c/lgy/GR00T-WholeBodyControl
pip install -e "gear_sonic/[training]"
python check_environment.py --training
```

`gear_sonic/pyproject.toml [training]` 里只声明了 hydra/wandb/trl/transformers/accelerate/tensorboard，运行时实际还会 import 几个**不在 extras 里**的包（见 §1.6）。

### 1.6 补装额外运行时依赖（**新人最常踩**）

`gear_sonic/[training]` 装完后启动训练会按顺序撞两个 `ModuleNotFoundError`：

```
ModuleNotFoundError: No module named 'open3d'
ModuleNotFoundError: No module named 'vector_quantize_pytorch'
```

`open3d` 在 `gear_sonic/utils/motion_lib/torch_humanoid_batch.py:18` 顶层被 import；`vector_quantize_pytorch.FSQ` 在 `gear_sonic/config/actor_critic/quantizers/fsq.yaml` 里被 hydra 实例化。

```bash
pip install open3d vector-quantize-pytorch
```

这两个包都体量不小（open3d ~500MB），国内网络下载慢的话用镜像：

```bash
pip install open3d vector-quantize-pytorch -i https://pypi.tuna.tsinghua.edu.cn/simple
```

验证：

```bash
python -c "import open3d; import vector_quantize_pytorch; print('extras OK')"
```

### 1.7 下载 release ckpt + SMPL 数据

```bash
pip install huggingface_hub
python download_from_hf.py --training
# 产物：sonic_release/last.pt  +  data/smpl_filtered/
```

### 1.8 验证整套环境（可选但推荐）

```bash
pytest tests/ -v -k "not sim_env"
```

预期 22 个 D1 测试通过；2 个 sim-only 测试需要 `.venv_sim`（mujoco + unitree_sdk2py），**与本流程无关**，跳过即可。

---

## 二、数据准备

整条数据流：**bones-SEED 原始 CSV → motion_lib PKL → 过滤 → 23DoF 清零**。三步后产出 `data/motion_lib_bones_seed/robot_filtered_23dof/`，那是 D1 训练实际加载的 motion_lib。

### 2.1 拿到 bones-SEED 原始 CSV

`data/bones_gmr/<日期目录>/<motion_name>/joint_data.csv`：每条 motion 一个目录，包含 G1 29DoF 关节角时间序列（120 fps）。

> 这一步本仓库不直接负责下载。Bones-SEED 不是公开数据集；如果你已经拿到 ≈ 142K 条 CSV 的 `bones_gmr/` 目录就直接跳到 §2.2。否则按 `docs/source/getting_started/installation_training.md` 跟内部团队拿。

### 2.2 CSV → motion_lib PKL（convert）

```bash
cd /media/woan/84a38787-1d4e-4ba7-892e-d1d90a009a8c/lgy/GR00T-WholeBodyControl

python -m gear_sonic.data_process.convert_soma_csv_to_motion_lib \
    --input  data/bones_gmr \
    --output data/motion_lib_bones_seed/robot \
    --fps 30 \
    --fps_source 120 \
    --individual \
    --num_workers 16
```

**要点：**

- `--input` 是**CSV 父目录的父目录**（多个日期子目录）；脚本会递归。
- `--individual` 让脚本一条 motion 一个 PKL（推荐；一个大 PKL 加载会爆内存）。
- 产出 PKL 用 **joblib** 压缩格式，schema：`{motion_name: {root_trans_offset, pose_aa, dof, root_rot, smpl_joints, fps}}`；其中 `dof` 形状 `(T, 29)`，列序遵循 IsaacLab 的 G1 29DoF 顺序（不是 MJCF！）。
- 142,220 条 CSV 在 16 worker 下约 30~60 分钟，产出 ≈ 8.6 GB。

验证：

```bash
ls data/motion_lib_bones_seed/robot/ | wc -l   # 应等于 motion 数 ≈ 142220
du -sh data/motion_lib_bones_seed/robot/
```

### 2.3 过滤（filter_and_copy_bones_data）

砍掉对训练有害或重复的 motion：

```bash
python -m gear_sonic.data_process.filter_and_copy_bones_data \
    --source data/motion_lib_bones_seed/robot \
    --dest   data/motion_lib_bones_seed/robot_filtered \
    --workers 16
```

默认 `--filter-keywords` 在脚本 `argparse` 里硬编码（见 `filter_and_copy_bones_data.py:208-252`），覆盖了一批已知会让训练崩的关键词。本机实测：142,220 → 129,785 条留下，磁盘 8.6G → 7.9G。

### 2.4 23DoF 清零（D1 路径专用）

把每条 PKL 里 `dof` 字段的 6 个 IL 列（waist_roll/pitch、L/R wrist_pitch/yaw）原位置零，写到新目录：

```bash
python -m gear_sonic.data_process.zero_23dof_motion_lib \
    --source data/motion_lib_bones_seed/robot_filtered \
    --dest   data/motion_lib_bones_seed/robot_filtered_23dof \
    --workers 16
```

**实测**：129,785 PKL → 7.5 GB，16 worker ≈ 30 分钟。

> **注意**：实际清零的是 `dof` 字段（IsaacLab 列序，索引 `MISSING_23DOF_INDICES_IL=[5,8,25,26,27,28]`），不是 `joint_pos`/`joint_vel`。`pose_aa` / `smpl_joints` / `root_*` 等其它字段保持原样。

### 2.5 完成后目录长这样

```
data/
├── bones_gmr/                          # 原始 CSV (§2.1)
├── motion_lib_bones_seed/
│   ├── robot/                          # convert 产物 (§2.2) ≈8.6GB / 142220 PKL
│   ├── robot_filtered/                 # 过滤后 (§2.3)     ≈7.9GB / 129785 PKL
│   └── robot_filtered_23dof/           # 6 列清零 (§2.4)   ≈7.5GB / 129785 PKL  ← 训练用这个
└── smpl_filtered/                      # release 自带     ≈30GB
```

> **没全量数据时**：`sample_data/robot_filtered/` 里有几条样本，先用它跑通 §三 的 Stage 0/1 smoke test，再补全量。

---

## 三、改动清单（D1 路径相对原 release 仓库的 diff）

### 3.1 新增文件

| 路径 | 作用 |
|---|---|
| `gear_sonic/utils/joint_constants.py` | **单一真值来源**：`MISSING_23DOF_INDICES_MJCF=[13,14,20,21,27,28]`（sim/deploy 列序）、`MISSING_23DOF_INDICES_IL=[5,8,25,26,27,28]`（IsaacLab 列序）、`MISSING_23DOF_JOINT_NAMES`（6 个关节名）。Sim/training/deploy 三处都从这里 import。 |
| `gear_sonic/utils/joint_mask.py` | 纯 Tensor 帮助函数 `apply_missing_dof_mask(actions, missing_idx)`，原地置零并返回。**不依赖 IsaacLab/pxr**，便于在 dev 机做单测。 |
| `gear_sonic/data_process/zero_23dof_motion_lib.py` | 离线把 motion_lib PKL 的 `dof` 字段（IsaacLab 列序）的 6 个 IL 列原位清零的预处理脚本（多进程，joblib 格式 in/out）。 |
| `gear_sonic/envs/wrapper/missing_dofs_env.py` | `MissingDofsLockEnv(ManagerBasedRLEnv)`：通过 monkey-patch `self.sim.step` 在每个物理 sub-step 之后调 `articulation.write_joint_state_to_sim` 把 6 个关节 qpos 钉死成 default、qvel=0。首次解析 joint_ids 时硬断言与 `MISSING_23DOF_INDICES_IL` 一致。 |
| `gear_sonic/config/missing_dofs/23dof_hardware.yaml` | hydra group config：`enabled: true`、`indices_mjcf`、`indices_il`、`joint_names`。 |
| `gear_sonic/config/exp/manager/universal_token/all_modes/sonic_release_23dof.yaml` | 实验 wrapper：继承 `sonic_release.yaml` + 启用 `missing_dofs.23dof_hardware` + 切换 motion lib 到 `robot_filtered_23dof/` + 把 obs term 的 `func` override 到 `*_masked` 变体 + 把 `joint_limit` 的 joint_names 从 `[".*"]` 改成显式列出的 23 个关节。 |

### 3.2 修改的现有文件

| 路径 | 改动 |
|---|---|
| `gear_sonic/utils/mujoco_sim/unitree_sdk2py_bridge.py` | `MISSING_23DOF_INDICES` 改为从 `joint_constants` import（不再就地定义）。 |
| `gear_sonic/utils/mujoco_sim/base_sim.py` | 同上，从共享模块 import。 |
| `gear_sonic/envs/manager_env/mdp/actions.py` | 从 `joint_mask` re-export `apply_missing_dof_mask`（保留向后兼容）。 |
| `gear_sonic/envs/manager_env/mdp/observations.py` | 新增 3 个 masked 变体：`joint_pos_rel_masked` / `joint_vel_rel_masked` / `joint_pos_multi_future_select_joints_masked`。stock 函数返回结果 → `.clone()` → 把 `MISSING_23DOF_INDICES_IL` 列置零。 |
| `gear_sonic/envs/wrapper/manager_env_wrapper.py` | `step(actions)` 在调 `self.env.step` **之前**根据 `config.missing_dofs.enabled` 条件触发 `apply_missing_dof_mask(env_actions, MISSING_23DOF_INDICES_IL)`。这一步是 `action_rate_l2` 自动正确的关键：mask 应用在 `action_manager.action` 落地之前，所以 prev/cur 在 6 个 idx 都是 0，diff² 必然 0。 |
| `gear_sonic/train_agent_trl.py` | 当 `config.missing_dofs.enabled=true` 时把 env class 切换到 `MissingDofsLockEnv`；否则保持 `ManagerBasedRLEnv`。 |

### 3.3 测试套件

| 路径 | 内容 | 是否依赖 IsaacLab |
|---|---|---|
| `tests/test_joint_constants.py` | 索引/名字常量校验 + sim bridge identity 检查 | 部分（identity 检查需 sim env） |
| `tests/test_zero_23dof_motion_lib.py` | 合成 joblib 压缩 PKL（含 `dof`、`pose_aa`、`smpl_joints`、`root_*`、`fps` 字段，匹配 convert 真实 schema）→ 跑预处理 → 验证 `dof` 6 IL 列被零、其它 23 列与其它字段全部不变 | 否 |
| `tests/test_missing_dofs_config.py` | hydra group YAML 一致性 | 否 |
| `tests/test_action_mask.py` | `apply_missing_dof_mask` 纯 Tensor 单测 | 否 |
| `tests/test_obs_mask.py` | AST 检查 3 个 `*_masked` 变体存在 + YAML `func:` 引用都有定义 + wrist-future 局部 idx 重映射逻辑 | 否 |
| `tests/test_reward_mask.py` | 数学验证：pre-mask 后 `action_rate_l2` 在 6 个 idx 的贡献为 0；源代码守卫：`ManagerEnvWrapper.step` 必须在 `self.env.step` 之前调 `apply_missing_dof_mask` | 否 |
| `tests/test_sonic_release_23dof_yaml.py` | YAML drift guard：obs func 都指向 `*_masked`；`joint_limit.joint_names` 不含 6 个 missing、覆盖全部 23 个 | 否 |

`groot_wbc` env 下 22 个测试通过，2 个 sim-only 测试需要 `.venv_sim`。

### 3.4 部署侧 / ONNX 完全不动

`gear_sonic_deploy/`、ONNX 导出、`deploy.sh`、C++ stack —— **零改动**。这是 D1 路径的核心约定：保 actor 维度 29 维，靠 SDK 默认 ignore 不存在的 motor index 实现真机兼容。

---

## 四、训练

### 4.1 进入环境

```bash
conda activate groot_wbc
cd /media/woan/84a38787-1d4e-4ba7-892e-d1d90a009a8c/lgy/GR00T-WholeBodyControl
export OMNI_KIT_ACCEPT_EULA=YES   # IsaacSim 启动必需
```

### 4.2 跑单元测试（dev 机也能跑）

```bash
pytest tests/ -v
```

预期：`groot_wbc` 下 22 passed / 2 failed（2 个失败是 sim-env 测试，需要 `.venv_sim`，与 D1 无关）。

### 4.3 Stage 0：跑 vanilla `sonic_release` baseline

D1 改动跑训练前先确认机器跑得动 vanilla 配置：

```bash
WANDB_MODE=offline python gear_sonic/train_agent_trl.py \
    +exp=manager/universal_token/all_modes/sonic_release \
    num_envs=16 headless=True \
    ++algo.config.num_learning_iterations=2 \
    ++manager_env.commands.motion.motion_lib_cfg.motion_file=data/motion_lib_bones_seed/robot_filtered \
    ++manager_env.commands.motion.motion_lib_cfg.smpl_motion_file=data/smpl_filtered
```

**通过条件**：跑完 2 个 iter，无 OOM、无 NaN reward。OOM 就降到 `num_envs=8`；还 OOM → 这台机器跑不动，止步。

**本机实测（RTX 4060 Laptop 8GB, num_envs=16）**：

| 指标 | iter 1 | iter 2 |
|---|---|---|
| 计算 throughput | 132 steps/s | 205 steps/s |
| Mean reward | 0.558 | 0.627 |
| Iteration time | 2.10s | 1.87s |

总耗时 4.77s，无 OOM、无 NaN，wandb offline 数据落 `/tmp/wandb/`。✅

### 4.4 Stage 1：D1 改动 smoke test

```bash
mkdir -p logs/d1_23dof
WANDB_MODE=offline python gear_sonic/train_agent_trl.py \
    +exp=manager/universal_token/all_modes/sonic_release_23dof \
    +checkpoint=sonic_release/last.pt \
    num_envs=16 headless=True \
    ++algo.config.num_learning_iterations=10 \
    ++manager_env.commands.motion.motion_lib_cfg.smpl_motion_file=data/smpl_filtered \
    2>&1 | tee logs/d1_23dof/stage1_d1_smoke.log
```

> `smpl_motion_file` 必须 override：`sonic_release_23dof.yaml` 只 override 了 `motion_file`，`smpl_motion_file` 还继承父 yaml 的 `data/bones_seed_smpl`，与 release 数据下载位置 `data/smpl_filtered` 不一致。

启动日志应能看到（验证 D1 改动都生效）：

- `MissingDofsLockEnvWrapper active` 或类似 — 物理锁开了
- 加载 motion lib 路径是 `data/motion_lib_bones_seed/robot_filtered_23dof`
- Reward 字段非 NaN，`tracking_relative_body_pos` 数量级合理

如果 reward 在 iter 0 就崩到 0 / NaN：先核对 `D1_23DOF_DESIGN_2026-05-15.md` §13.4 的 IL idx → joint name 映射，再看 `joint_constants.py:MISSING_23DOF_INDICES_IL` 是否一致。

### 4.5 Stage 2：二分 `num_envs`

4060 8GB 上从 16 起几何倍增到 OOM：

| num_envs | 命令 |
|---|---|
| 16 | 已在 Stage 1 |
| 64 | `num_envs=64 ++algo.config.num_learning_iterations=3` |
| 128 | 同上换 128 |
| 256 → 512 → 1024 | 直到 OOM |

边跑边在另一终端 `nvidia-smi -l 1` 看 VRAM 峰值，记录最大可行的 `num_envs`。

**本机实测（RTX 4060 Laptop 8GB）**：

| num_envs | VRAM 稳态 | 状态 |
|---|---|---|
| 512 | ≈ 6 GB | ✅ |
| 800 | 推算 ≈ 7.7 GB（+0.3-0.5 GB PPO 峰值） | ✅ 边缘可行 |
| 1024 | 推算 ≈ 9 GB | ❌ OOM |

→ 选 **`num_envs=800`** 进 Stage 3。OOM 时退 768。

### 4.6 Stage 3：本地两天训练

```bash
tmux new -s d1train
conda activate groot_wbc
cd /media/woan/84a38787-1d4e-4ba7-892e-d1d90a009a8c/lgy/GR00T-WholeBodyControl
export OMNI_KIT_ACCEPT_EULA=YES         # IsaacSim 启动必需，新 shell 要重新 export

mkdir -p logs_rl
WANDB_MODE=offline python gear_sonic/train_agent_trl.py \
    +exp=manager/universal_token/all_modes/sonic_release_23dof \
    +checkpoint=sonic_release/last.pt \
    num_envs=800 headless=True \
    ++manager_env.commands.motion.motion_lib_cfg.smpl_motion_file=data/smpl_filtered \
    2>&1 | tee logs_rl/d1_2day.log

# Ctrl+B D 脱离；tmux attach -t d1train 回去看；tmux ls 列 session
```

> **每个参数为什么在那里**：
> - `tmux new -s d1train`：把训练放后台 session，关终端/SSH 断/笔记本盖上都不影响进程，`tmux attach -t d1train` 能回去看
> - `export OMNI_KIT_ACCEPT_EULA=YES`：IsaacSim 5.x 启动时检查的环境变量，没设会启动失败。`.bashrc` 里没加的话每个新 shell 都得重设
> - `WANDB_MODE=offline`：本地训练不联网传 wandb 云端，数据落 `wandb/offline-run-*/`，需要时 `wandb sync` 上传
> - `+exp=...sonic_release_23dof`：D1 实验配置（继承 `sonic_release.yaml` + 6 项 23DoF 改动）
> - `+checkpoint=sonic_release/last.pt`：从 release 29DoF 模型 finetune（不是 from-scratch）
> - `num_envs=800`：4060 8GB 上的边缘可行值，参考 §4.5 决策
> - `headless=True`：无 GUI 模式，省 VRAM、不弹窗
> - `++manager_env.commands.motion.motion_lib_cfg.smpl_motion_file=data/smpl_filtered`：**必须**。yaml 里硬编码是 `data/bones_seed_smpl`，但 release 数据下载到 `data/smpl_filtered`，不 override 会找不到 SMPL 数据
> - `motion_file` 不需要 override：`sonic_release_23dof.yaml` 已经写死指向 `robot_filtered_23dof`
> - `2>&1 | tee logs_rl/d1_2day.log`：stderr 合并到 stdout，再分流到日志文件
>
> 默认值不用动的：`seed=0`、`algo.config.num_learning_iterations=100000`（48 小时跑不完，手动 Ctrl+C 即可）、`algo.config.save_interval=500`（每 500 iter 存一次 ckpt）。

定期检查（不用每小时）：

```bash
tail -50 logs_rl/d1_2day.log
ls -la logs_rl/TRL_G1_Track/ | tail -5
nvidia-smi
```

监控点：NaN reward、crash、GPU 温度、磁盘占用。

48 小时后停训，画曲线：

```bash
tensorboard --logdir logs_rl/TRL_G1_Track/ --bind_all
```

### 4.7 验收

参考 `D1_23DOF_DESIGN_2026-05-15.md` §7.1：

| 准则 | 必须 |
|---|---|
| 启动日志有 6 missing dofs masked + reference filtered + physics locked | ✅ |
| iter 0 reward 非 NaN | ✅ |
| ≥ 100 iter 无 OOM / segfault | ✅ |
| `tracking_relative_body_pos` 前 100 iter 不单调下降 | ✅ |
| TensorBoard / W&B 曲线正常记录 | ✅ |

> 4060 上的 reward **不需要"上涨到收敛值"**。本地两天产出是「代码可执行 + 框架稳定」，不是收敛模型。

### 4.8 云端正式训练

本地 Stage 1-3 通过后再上云。8 卡 A100/H100 推荐配置：

```bash
accelerate launch --num_processes=8 gear_sonic/train_agent_trl.py \
    +exp=manager/universal_token/all_modes/sonic_release_23dof \
    +checkpoint=sonic_release/last.pt \
    num_envs=4096 headless=True
```

最终验收（参考 design doc §7.2）：

- `success_rate > 0.95`
- `mpjpe_l < 30 mm`（排除 6 个 mask idx）
- sim2sim 锁关节验证 ≥ 30 s 不漂移
- 真机吊起空载站立 ≥ 1 分钟不晃

### 4.9 关掉 D1 改动 / 回到 vanilla

把 `+exp=...sonic_release_23dof` 换回 `+exp=...sonic_release`。所有 D1 改动都通过 hydra `config.missing_dofs.enabled` 闸门控制，关掉就完全等价于原 29DoF 训练。

---

## 五、单一 hydra 开关原则

所有 6 项 D1 改动由 hydra 的 `+missing_dofs=23dof_hardware` 一键打开（在 `sonic_release_23dof.yaml` 里默认引入），全部源于这一个 group 的 `enabled: true`：

| 改动 | 触发位置 | 受控变量 |
|---|---|---|
| 1. 物理锁 6 关节 | `train_agent_trl.py:149-156` | `config.missing_dofs.enabled` 决定 env class 是 `MissingDofsLockEnv` 还是 `ManagerBasedRLEnv` |
| 2. action mask | `manager_env_wrapper.py:848-849` | `config.missing_dofs.enabled` 决定是否 pre-step mask |
| 3. obs mask | `sonic_release_23dof.yaml` 里 obs term `func:` override 到 `*_masked` | YAML override |
| 4. reward mask | `sonic_release_23dof.yaml` 里 `joint_limit.params.asset_cfg.joint_names` 改 23 项；`action_rate_l2` 由 (2) 隐式生效 | YAML override |
| 5. reference motion 切到 23dof | `sonic_release_23dof.yaml` 里 `motion_file` override | YAML override |
| 6. ckpt 加载 | 不动代码，用 `+checkpoint=sonic_release/last.pt` | 维度不变直接 finetune |

---

## 六、常见问题

**Q: 装完 `gear_sonic[training]` 启动训练秒崩 `ModuleNotFoundError: open3d` 或 `vector_quantize_pytorch`？**
A: 这两个 import 不在 `[training]` extras 里。`pip install open3d vector-quantize-pytorch`，详见 §1.6。

**Q: 跑 `zero_23dof_motion_lib` 报 `_pickle.UnpicklingError: invalid load key, 'x'`？**
A: 旧版本脚本用 `pickle.load`，但 convert 写出的是 `joblib.dump(..., compress=True)`（zlib 压缩格式）。你的本地仓库需保证脚本里用的是 `joblib.load/dump`（当前版本已修）。

**Q: 训练时 `Articulation has no attribute 'write_joint_state_to_sim'`?**
A: IsaacLab 版本不对。必须是 v2.3.2。检查：`python -c "import isaaclab; print(isaaclab.__version__)"`。

**Q: 启动报 `joint 'waist_pitch_joint' not in articulation joint_names`?**
A: USD 模型不是预期的 G1 29DoF。`MissingDofsLockEnv._resolve_missing_joint_state` 会硬断言 6 个关节名都在 `articulation.data.joint_names` 里。检查所用 robot config 是否切到了简化版。

**Q: 报 `resolved missing joint ids ... != MISSING_23DOF_INDICES_IL [5, 8, 25, 26, 27, 28]`?**
A: IsaacLab 的 G1 关节顺序变了，与本仓库 `gear_sonic/envs/env_utils/joint_utils.py:G1_ISAACLab_ORDER` 不一致。打印 `articulation.data.joint_names` 看新顺序，再回头改 `joint_constants.py:MISSING_23DOF_INDICES_IL`，并同步 `D1_23DOF_DESIGN_2026-05-15.md` §13.4 的表。

**Q: reward 在 iter 0 就 NaN？**
A: 极大概率是 ckpt 维度对不上 / IL idx 列对错了。先在 `num_envs=4` 跑一遍，print `env_actions[:, MISSING_23DOF_INDICES_IL]`，确认这 6 列在 mask 后真的是 0。再确认 motion lib 加载的是 `robot_filtered_23dof` 而不是 `robot_filtered`。

**Q: 4060 8GB 二分 `num_envs` 二分到几才合理？**
A: 项目原配是在 ≥ 48GB 卡上跑 4096 envs。8GB 经验上落在 128~256，跑下来 throughput 偏低，但本地的目的是「框架稳定」不是「训出 SOTA」。

**Q: 把 release 模型微调到「6 关节锁死」分布，前 100 iter reward 是不是必跌？**
A: 是。release 模型在 6 关节自由上训的，初期 OOD 会导致 reward 显著下跌。design §10.2 的预案：跌到 < 1.0 可以考虑 from-scratch 重训而不是 finetune。本地两天主要看「能不能稳住学习并慢慢回升」，不是「跌不跌」。

**Q: 真机 23DoF 上 motor_cmd[13/14/20/21/27/28] 会不会被 SDK 错误下发？**
A: Unitree SDK 默认 ignore 不存在的 motor index。但**真机上要现场二次确认**——参考 `23DOF_EVALUATION_2026-05-14.md` §10.1。

---

## 七、完成度

D1 plan / design 已实施落地的部分：

- 环境与依赖：本文 §一
- 数据准备：本文 §二（实测 142,220 → 129,785 → 129,785，磁盘 8.6→7.9→7.5 GB）
- Phase 1（4 项探针）：`D1_23DOF_DESIGN_2026-05-15.md` §13
- Phase 2（probe-independent code）：commits `3087c45 ed0ac8e dd04090 5fe428a 55f9275 8e9f9f8`
- Phase 3（probe-dependent code）：commits `22bcc1a 8e9f9f8`
- 22 个 D1 单测在 `groot_wbc` 通过
- ✅ Phase 0.4 baseline smoke（§4.3 实测：2 iter, 4.77s, reward 0.558→0.627）

仍待 GPU 实跑产出（本文 §4.4-4.7）：

- Phase 4.1 D1 smoke
- Phase 4.2 num_envs 二分
- Phase 4.3 两天本地训练
- Phase 4.4 写 `D1_23DOF_TRAINING_2026-05-15.md` 实测日志

跑完上面 4 步、产出训练日志，D1 路径的本地侧就完整闭环；之后即可上云端真训练。
