# D1 路径：23DoF G1 真机部署的训练侧微调设计

> 设计日期：2026-05-15
> 上游：`23DOF_EVALUATION_2026-05-14.md`（已证明 zero-out 路径不可行）
> 目的：把 release 29DoF 模型 fine-tune 出一个**输出 29 维但其中 6 维永远 ≈ 0** 的策略，使其能直接上 23DoF G1 真机
> 作用域：本文件只覆盖**训练侧**（含本地 4060 上的 smoke test 与云端正式训练的设计）。部署侧 C++/ONNX 链路完全不动。

---

## 0. TL;DR

- **可以上真机，且不需要改 deploy 侧任何代码**——前提是：训练时除了 mask action / observation，还必须**物理锁住** sim 中的这 6 个关节，并把 reference motion / reward 都对齐
- "只 mask 不锁物理"等价于上次 5-14 评估实验，已知失败（重心持续前移），见 `23DOF_EVALUATION_2026-05-14.md` §6.2
- 训练侧需要 6 项改动；部署侧 0 项；ONNX 维度保持 29 维
- 4060 8GB 上跑 `num_envs=1024` 不可行（项目用 4096 envs 跑在 ≥48GB 卡上）；本地两天的目标是**验证 D1 改动正确性**与**reward 不立刻崩盘**，不是收敛

---

## 1. 上真机的兼容性论证（回答"会不会维度错误"）

**结论：不会维度错误。**

### 1.1 链路盘点

| 层 | 维度 | 是否需要改 |
|---|---|---|
| Actor 网络输出 | 29 | ❌ 不改 |
| ONNX `*_g1.onnx` action_dim | 29 | ❌ 不改 |
| C++ deploy 端 `G1_NUM_MOTOR` | 29 | ❌ 不改 |
| Unitree SDK2 在 23DoF 真机上的 motor 数组 | 29（向下兼容；其中 6 个对应"物理不存在"） | ❌ 不改 |
| `motor_cmd[13/14/20/21/27/28]` 在真机的处理 | SDK 静默 ignore，不报错 | ❌ 不改 |

### 1.2 为什么这条路 sim 已经验证过

`23DOF_EVALUATION_2026-05-14.md` 实测了 `G1_NUM_MOTOR=29 + ONNX 29 维 → mujoco_sim 虚拟拔关节`，链路完全跑通，**没有任何维度错误**。失败原因是 OOD（release 模型用 waist_pitch 平衡），与维度无关。

### 1.3 为什么"只 mask 不动 actor 维度"是正确选择

| 选项 | 优势 | 劣势 |
|---|---|---|
| **保持 actor 29 维 + 训练时 mask 后 6 维 ≡ 0**（本设计） | deploy 侧 0 改动；ckpt 维度不变可直接 finetune；ONNX 自动兼容 | actor 仍学习 6 维分布（mask 在末端），训练效率略低 |
| 改 actor 输出 23 维 + deploy 侧扩 0 到 29 | 网络容量更专注 | 需要改 actor 头部 / log_prob / ckpt 加载手动 reshape；ONNX 需要重新设计扩 0 层；deploy 侧也要改 |

D1 选第一条。这与 `23DOF_EVALUATION_2026-05-14.md` §10.1 的部署侧约束（"motor_cmd[13/14/20/21/27/28] 不能被 SDK 错误下发"）一致。

---

## 2. Stage 0：环境准备（一次性，本地）

### 2.1 现状

| 项 | 实测 |
|---|---|
| GR00T-WBC 要求的 IsaacLab | **2.3.2**（README badge）/ **2.3+**（`installation_training.md`） |
| GR00T-WBC 要求的 Python | 3.11 |
| 现有 `IsaacLab-v2.1.0` 文件夹 | `VERSION` 文件实际是 **2.0.2**，是 Beyondmimic 用的，**保留不动** |
| 本地 GPU | NVIDIA RTX 4060 Laptop, **8 GB VRAM** |

### 2.2 安装步骤

1. 在 GitHub 官方 `isaac-sim/IsaacLab` 上 fork **v2.3.2 tag** 对应的 commit 到自己的仓库
2. 本地 clone 到 `/media/woan/84a38787-1d4e-4ba7-892e-d1d90a009a8c/lgy/IsaacLab-v2.3.2/`
3. 按官方安装流程（IsaacSim 5.x + IsaacLab pip extension）：
   - 创建独立 conda env：`conda create -n groot_wbc python=3.11`
   - 装 IsaacSim（pip 或 omniverse launcher）
   - `cd IsaacLab-v2.3.2 && ./isaaclab.sh -i`
4. 验证：
   ```bash
   conda activate groot_wbc
   python -c "import isaaclab; print(isaaclab.__version__)"  # 应输出 2.3.2
   ```
5. 装 gear_sonic 训练依赖：
   ```bash
   cd /media/.../GR00T-WholeBodyControl
   pip install -e "gear_sonic/[training]"
   python check_environment.py --training
   ```

### 2.3 与 Beyondmimic 环境的隔离

- Beyondmimic 用 `.venv_*` 或它自己的 conda env，跟 GR00T 用的 `groot_wbc` env 完全隔离
- 不同项目跑时 `conda activate` 切换即可
- 两份 IsaacLab 文件夹并列存在，不冲突

---

## 3. Stage 1：共享常量

复用 sim 端已存在的 `MISSING_23DOF_INDICES = [13, 14, 20, 21, 27, 28]`（在 `gear_sonic/utils/mujoco_sim/unitree_sdk2py_bridge.py:26`）。

**做法**：把这个常量提到一个上层模块（`gear_sonic/utils/joint_constants.py`），让 sim 端、training 端、deploy reference 三个地方都从这里 import。**单一真值来源**。

注意 IL 列序与 MJCF 列序的差别：
- MJCF idx：`[13, 14, 20, 21, 27, 28]`（sim / deploy）
- IL idx：`[5, 8, 25, 26, 27, 28]`（reference CSV 列序，见 `gear_sonic_deploy/reference/gmr_to_deploy.py` 的 `MJ_TO_IL`）

训练端使用哪种 idx 取决于 IsaacLab 内部用的关节顺序（**待环境装好后确认**，这是 §10 风险项）。

---

## 4. Stage 2：6 项训练侧改动

### 4.1 改动 1（核心）：IsaacLab Articulation 物理锁关节

**目的**：让 sim 端这 6 个关节物理上"不存在"——每个 RL step 之后把它们的 `qpos` 钉死成 default、`qvel = 0`。

**对应 sim 侧做法**：上次评估时在 `gear_sonic/utils/mujoco_sim/base_sim.py:_pin_missing_23dof_joints` 用 MuJoCo `data.qpos[adr] = qpos0; data.qvel[adr] = 0` 实现。IsaacLab 端的等价做法是在 `ManagerBasedRLEnv.step()` 之后调用 `articulation.write_joint_state_to_sim(qpos, qvel, joint_ids=missing_ids)`。

**为什么这一步是核心**：上次评估实验已经证明，只 mask action / obs 而不锁物理 → 等价于让策略命令"被吃掉"但物理状态仍 free → 策略 loop 就崩了。物理锁是把"关节物理不存在"这个语义植入仿真。

**待确认**（§10 风险项）：
- IsaacLab 2.3.2 `Articulation.write_joint_state_to_sim` 的精确签名
- 在 `ManagerBasedRLEnv` 哪个 hook 调用最合适（`_post_step` / `_apply_action` / `_pre_physics_step`）
- 性能开销（每步多一次 GPU↔CPU 同步会不会显著掉 throughput）

**回退方案**：如果硬复位 API 不可用 / 性能太差，改用大 stiffness + zero target（软焊死）。语义略弱但兼容性高。

### 4.2 改动 2：Action mask

**目的**：actor 输出 (B, 29) → 把 6 个 idx 写 0 → 再送进 articulation。

**位置**：`gear_sonic/envs/manager_env/mdp/actions.py`。具体改动点是 `JointPositionAction` / `JointTargetAction` 之类的 action term：在它的 `apply_actions(self, actions)` / `process_actions` 入口处 `actions[:, MISSING_23DOF_INDICES] = 0`。

**做成可配置**：通过 `ActionTermCfg.params["mask_missing_23dof"] = True/False`，默认 False（不影响原训练）。23DoF 训练时通过 hydra override 打开。

### 4.3 改动 3：Observation mask

**目的**：observation 里的 `joint_pos` / `joint_vel` 这 6 个 idx 写 0。匹配真机这 6 个 motor_state 的反馈（真机也是 0）。

**位置**：`gear_sonic/envs/manager_env/mdp/observations.py`。具体改 `joint_pos` / `joint_vel` term 的实现，或者在 term wrapper 里加 mask。

**注意**：项目里有多种 obs term（`joint_pos`, `joint_pos_wo_hand`, `joint_pos_multi_future` 等），见 `observations.py:108-432`。需要先确认 sonic_release 实际启用的是哪几个，再决定 mask 全部还是只 mask 主 obs。**待环境装好后跑 smoke test 看打印的 obs 维度**。

### 4.4 改动 4：Reward mask

**目的**：tracking reward（`tracking_relative_body_pos`、`tracking_local_joint_pos` 等）计算 joint-level error 时排除这 6 个 idx。否则会把"actor 输出 0"罚成"跟踪失败"。

**位置**：`gear_sonic/envs/manager_env/mdp/rewards.py`。具体改动点取决于 reward term 实现：
- 若 reward 是 per-joint MSE 后 mean，则把 mask idx 处的 error 设为 0 再 mean
- 若是 weighted sum，把 6 个 idx 的 weight 设为 0

**待确认**（§10）：sonic_release 用的 tracking reward term 名称和实现路径。

### 4.5 改动 5：Reference motion mask

**目的**：训练用的 motion lib（PKL）这 6 个 IL 列也清零，让 reference target 与"actor 输出 0"自洽，否则 reward 仍会驱使策略去 imitate 不可能完成的动作。

**两种实现**：

| 方案 | 优势 | 劣势 |
|---|---|---|
| **预处理**：写 `gear_sonic/data_process/zero_23dof_motion_lib.py`，从 `data/motion_lib_bones_seed/robot_filtered/` 生成 `robot_filtered_23dof/` | 干净，与 deploy 侧 `zero_23dof_columns.py` 同思路 | 多一份磁盘空间（~130K PKL × 2） |
| 运行时 mask：在 motion lib loader 里 mask | 不占磁盘 | 改的位置更深，运行时开销 |

**选预处理方案**——和 deploy 侧（`gear_sonic_deploy/reference/zero_23dof_columns.py`）保持一致。

### 4.6 改动 6：Checkpoint 加载

**不改代码**。维度不变 → `+checkpoint=sonic_release/last.pt` 直接 finetune。

actor / critic 网络维度都没变（仍 29 维输入 / 输出），加载零摩擦。

---

## 5. Stage 3：启动开关与配置

### 5.1 单一开关原则

加一个 hydra config flag `+missing_dofs=23dof_hardware`，默认 unset。打开时同时启用：
- 改动 1 物理锁
- 改动 2 action mask
- 改动 3 obs mask
- 改动 4 reward mask
- 改动 5 reference path 切到 `robot_filtered_23dof/`

### 5.2 Wrapper 配置文件

新建 `gear_sonic/config/exp/manager/universal_token/all_modes/sonic_release_23dof.yaml`：
- 继承 `sonic_release.yaml`
- override `missing_dofs: 23dof_hardware`
- override motion lib 路径

### 5.3 启动命令模板

```bash
# 4060 上的 smoke test（验证不报错）
python gear_sonic/train_agent_trl.py \
    +exp=manager/universal_token/all_modes/sonic_release_23dof \
    +checkpoint=sonic_release/last.pt \
    num_envs=16 headless=True \
    ++algo.config.num_learning_iterations=10

# 本地两天跑（num_envs=MAX，MAX 由 §8.1 二分查找得到，写回本文件 §10.2）
python gear_sonic/train_agent_trl.py \
    +exp=manager/universal_token/all_modes/sonic_release_23dof \
    +checkpoint=sonic_release/last.pt \
    num_envs=<MAX> headless=True

# 云端正式训练（A100/H100 ×8）
accelerate launch --num_processes=8 gear_sonic/train_agent_trl.py \
    +exp=manager/universal_token/all_modes/sonic_release_23dof \
    +checkpoint=sonic_release/last.pt \
    num_envs=4096 headless=True
```

---

## 6. Stage 4：部署侧

**完全不动**。

- C++ deploy stack（`gear_sonic_deploy/`）：不改
- ONNX 导出脚本：不改
- `deploy.sh`：不改
- 真机上 SDK 对 6 个 missing motor 的处理：依赖 SDK 本身的 ignore 行为，已在 `23DOF_EVALUATION_2026-05-14.md` §10.1 提到需要现场验证

---

## 7. Stage 5：验收标准

### 7.1 本地两天（4060 8GB）的成功定义

| 准则 | 必须满足 |
|---|---|
| `python gear_sonic/train_agent_trl.py +exp=...sonic_release_23dof` 启动不报错 | ✅ |
| 启动日志能看到"6 missing dofs masked + reference filtered + physics locked" | ✅ |
| Iteration 0 reward 输出非 NaN | ✅ |
| 至少跑过 100 iteration，没 OOM 没 segfault | ✅ |
| `tracking_relative_body_pos` reward 在前 100 iter 不单调下降 | ✅ |
| TensorBoard / W&B 能正常记录曲线 | ✅ |

**注**：4060 上跑 smoke 量级，**reward 不需要"上涨到收敛值"**。这两天的产出是"代码改动可执行"+"训练框架稳定"。

### 7.2 云端正式训练（如果本地通过）的最终验收

| 准则 | 目标值 | 来源 |
|---|---|---|
| `success_rate` | > 0.95 | `training.md` Eval Metrics |
| `mpjpe_l`（排除 6 个 mask idx） | < 30 mm | 同上 |
| sim2sim 锁关节验证：`python gear_sonic/scripts/run_sim_loop.py --simulate-23dof` + ONNX，能 `T` 起步走完 penguin_walking 不前移 | ≥ 30 s 不漂移 | `23DOF_EVALUATION_2026-05-14.md` §1.3 |
| 真机吊起来空载站立 | ≥ 1 分钟不晃 | 同上 |

---

## 8. 本地两天的具体跑法（4060 8GB）

### 8.1 显存二分查找 num_envs

从 `num_envs=16`（项目默认 smoke）开始，倍增直到 OOM：
```
16 → 32 → 64 → 128 → 256 → 512 → 1024
```
8 GB 4060 大概率在 128~256 之间触底。记录最大可行 `num_envs` 写到本文件 §10。

### 8.2 跑两天

用 §8.1 找到的最大 `num_envs`，开 W&B offline 模式（避免上传）：
```bash
WANDB_MODE=offline python gear_sonic/train_agent_trl.py \
    +exp=manager/universal_token/all_modes/sonic_release_23dof \
    +checkpoint=sonic_release/last.pt \
    num_envs=<MAX> headless=True
```

预期：两天能跑 ~5K-20K iter（具体看 throughput），看 reward 曲线趋势是否稳定不崩。

---

## 9. 输出物清单

| # | 文件 | 类型 | 何时产出 |
|---|---|---|---|
| 1 | `D1_23DOF_DESIGN_2026-05-15.md` | 设计 spec（本文件） | 现在 |
| 2 | `gear_sonic/utils/joint_constants.py` | 新增模块 | Stage 1 |
| 3 | `gear_sonic/envs/manager_env/mdp/actions.py` | 改 | Stage 2.2 |
| 4 | `gear_sonic/envs/manager_env/mdp/observations.py` | 改 | Stage 2.3 |
| 5 | `gear_sonic/envs/manager_env/mdp/rewards.py` | 改 | Stage 2.4 |
| 6 | `gear_sonic/envs/manager_env/wrapper/<env_subclass>.py` 或类似 | 新增/改 | Stage 2.1 物理锁 |
| 7 | `gear_sonic/data_process/zero_23dof_motion_lib.py` | 新增 | Stage 2.5 |
| 8 | `gear_sonic/config/exp/manager/universal_token/all_modes/sonic_release_23dof.yaml` | 新增 | Stage 3 |
| 9 | `D1_23DOF_TRAINING_2026-05-15.md` | 改动汇总+本地实跑结果 | Stage 5 完成后 |
| 10 | 训练 ckpt + ONNX | 训练产出 | 云端训练后 |

---

## 10. 风险与未知项

### 10.1 装环境后必须立即核实的 4 项事实

| # | 事实 | 影响 | 怎么核实 |
|---|---|---|---|
| 1 | IsaacLab 2.3.2 `Articulation.write_joint_state_to_sim` 的精确签名与可用 hook 位置 | 改动 1 实现细节 | `python -c "from isaaclab.assets import Articulation; help(Articulation.write_joint_state_to_sim)"` |
| 2 | sonic_release 训练时实际启用的 obs term 列表 | 改动 3 mask 范围 | 跑 smoke test 看启动日志 |
| 3 | sonic_release 用的 tracking reward term 名称与实现 | 改动 4 mask 范围 | grep `gear_sonic/config/manager_env/rewards/` |
| 4 | IsaacLab 内部用的 G1 关节顺序与 MJCF idx 是否一致 | mask 用哪套 idx | `articulation.joint_names` 打印对照 |

### 10.2 已知风险

| 风险 | 缓解 |
|---|---|
| 4060 8GB 上 num_envs 太小 → 训不出像样曲线 | 接受。本地只验证 pipeline，云端跑真训练 |
| 物理锁性能开销可能拖慢 throughput | 改用大 stiffness 软锁回退 |
| release ckpt 是在"6 关节自由"分布上训出来的，从它 finetune 到"6 关节锁死"分布可能初期 reward 大幅下跌 | 监控前 100 iter，如果 reward 跌到 < 1.0 考虑退到 from-scratch 训练 |
| `anneal_23dof` mask 在 sim 部署 yaml 里是 `0 waist + 6 wrist` 与硬件 23DoF 不一致 | 本设计**不复用**它，从训练侧重新定义 mask（直接用 MJCF idx 列表），与 sim 部署 yaml 解耦 |

### 10.3 范围外（不在本设计内）

- C++ deploy 修改：不动
- ONNX 导出修改：不动
- 真机 SDK 对 6 个 missing motor 行为验证：现场做（参考上次评估 §10.1）

---

## 11. 与上次评估实验的关系

| 上次（5-14） | 本次（5-15） |
|---|---|
| 仅 sim 端虚拟拔关节 + 推理时 zero-out | 训练侧 fine-tune（D1 路径） |
| 用 release ckpt **直接** sim2sim | 用 release ckpt **作为 init** 微调 |
| 失败：重心持续前移 | 目标：让策略学会"6 关节不可用" |
| 改动 4 文件 + 1 新文件（sim 侧） | 改动 3 个 mdp 文件 + 1 个 env wrapper + 3 个新文件（`joint_constants.py`、`zero_23dof_motion_lib.py`、`sonic_release_23dof.yaml`）（训练侧） |
| 部署侧不动 | 部署侧不动 |
| MJCF idx 列序 | 训练侧需要确认 IsaacLab idx 列序（§10.1） |

本次设计在上次评估的"必须走训练侧 fine-tune"结论上展开。本次本地两天的 smoke test 等价于把上次的 sim 验证从 deploy 侧搬到训练侧。

---

## 12. 执行顺序（plan 阶段交付）

1. Stage 0：装 IsaacLab 2.3.2 + groot_wbc env
2. 核实 §10.1 四项事实
3. Stage 1：抽 `joint_constants.py`
4. Stage 2.1：实现物理锁（**先做这一项，其它依赖它**）
5. Stage 2.2-2.4：mask 改动
6. Stage 2.5：跑 `zero_23dof_motion_lib.py` 生成 23dof 参考数据
7. Stage 3：写 `sonic_release_23dof.yaml`
8. Stage 5.1：smoke test 跑通
9. Stage 8.1：二分 num_envs
10. Stage 8.2：起两天训练
11. 写 `D1_23DOF_TRAINING_2026-05-15.md` 汇总实测改动行号 / 实测 num_envs / reward 曲线截图

详细 plan 在 brainstorming 通过后由 writing-plans skill 生成。

---

## 13. Probe Results (Phase 1, 2026-05-18)

四项探针结果在 groot_wbc env 下取得，与 §10.1 四个未知项一一对应。

### 13.1 IsaacLab Articulation API & ManagerBasedRLEnv hook (Task 1.1)

**Articulation hard-pin API（首选）：**

```python
Articulation.write_joint_state_to_sim(
    position: torch.Tensor,
    velocity: torch.Tensor,
    joint_ids: Sequence[int] | slice | None = None,
    env_ids: Sequence[int] | slice | None = None,
)
```

文档原文："Write joint positions and velocities to the simulation. The API directly writes the joint states (i.e. positions and velocities) to the simulator. It also resets the joint state buffers to maintain consistency."

**辅助 API**：
- `Articulation.write_joint_position_to_sim(position, joint_ids, env_ids)`
- `Articulation.write_joint_velocity_to_sim(velocity, joint_ids, env_ids)`
- `Articulation.set_joint_position_target(target, joint_ids, env_ids)`（设置 buffer，需要后续 `write_data_to_sim` 才生效；不适合"硬钉死"）
- `Articulation.set_joint_velocity_target(...)`（同上）

**hook 选择**：`ManagerBasedRLEnv.step()` 没有 `_pre_physics_step` / `_post_physics_step` 这种独立 hook，整个流程是单体 `step()`：
1. `action_manager.process_action(action)`
2. for `decimation` 次：`action_manager.apply_action()` → `scene.write_data_to_sim()` → `sim.step()` → `scene.update()`
3. termination/reward/reset/observation 计算

**结论 — 推荐做法**（避免 subclass `step()`）：把"硬钉死 6 个 missing 关节"做成一个 `EventTermCfg`，绑定 `mode="interval"` 且 `interval_range_s=(0, 0)`（每个 sim step 都触发）。EventTerm 拿到 env 直接 `articulation.write_joint_state_to_sim(qpos0, 0, joint_ids=missing_il_ids)`。

**回退**：如果 EventTerm 在 `interval=0` 时不能在 `apply_action` 之后/`sim.step` 之前触发（实测确认），则 subclass `ModularTrackingEnvCfg` 的 env class，override `step()`：在每次 `for _ in range(self.cfg.decimation)` 内的 `sim.step()` **之后**调用 `articulation.write_joint_state_to_sim(...)`。

实际选择放到 Phase 3 Task 3.4 实现时落实——两条路都已验证 API 可用。

### 13.2 sonic_release 启用的 obs terms（Task 1.2）

`gear_sonic/config/exp/manager/universal_token/all_modes/sonic_release.yaml` 通过 defaults 引用：
- `manager_env/observations/policy: local_dir_hist`
- `manager_env/observations/critic: privileged_mf_hist`
- `manager_env/observations/tokenizer: unitoken_all_noz`

**含 joint state 的 term**：

| group | term name | 实现 | 维度 |
|---|---|---|---|
| policy | `joint_pos` | `isaaclab.envs.mdp.joint_pos_rel`（stock） | 29 |
| policy | `joint_vel` | `isaaclab.envs.mdp.joint_vel_rel`（stock） | 29 |
| critic | `joint_pos` | 同上 | 29 |
| critic | `joint_vel` | 同上 | 29 |
| tokenizer | `joint_pos_multi_future_wrist_for_smpl` | `gear_sonic.envs.manager_env.mdp:joint_pos_multi_future_select_joints_for_smpl` | 6（仅 wrist，IL idx 23-28） |

`joint_pos_rel` / `joint_vel_rel` 实现（IsaacLab stock）：

```python
asset.data.joint_pos[:, asset_cfg.joint_ids] - asset.data.default_joint_pos[:, asset_cfg.joint_ids]
```

这两个返回 shape `(B, 29)`，6 个 missing IL idx 物理被钉死后值≈0；为安全起见，仍要在 obs term 出口处 mask 这 6 列。

**Mask 插入点**（Phase 3 Task 3.2）：
- 在 `gear_sonic/envs/manager_env/mdp/observations.py` 新增 `joint_pos_rel_masked` / `joint_vel_rel_masked`，调用 stock 函数后 `out[:, MISSING_23DOF_INDICES_IL] = 0`
- 在 23dof YAML wrapper `sonic_release_23dof.yaml` override `joint_pos.func` / `joint_vel.func` 指到新函数

`joint_pos_multi_future_wrist_for_smpl` 的 `joints_idx: [23, 24, 25, 26, 27, 28]` 包含 4 个 missing wrist（IL idx 25, 26, 27, 28 都在内）；这一项也需要 mask（在 wrapper YAML 把对应位置的 channel 清零，或者也走自定义函数包一层）。

### 13.3 sonic_release 启用的 reward terms（Task 1.3）

`manager_env/rewards: tracking/base_5point_local_feet_acc` 由 `defaults` 列出的 12 个 reward term 组成，其中 **per-joint** 项：

| term | weight | 实现 | 是否需要 mask |
|---|---|---|---|
| `tracking_anchor_pos` | 0.5 | gear_sonic | 否（pelvis 单点） |
| `tracking_anchor_ori` | 0.5 | gear_sonic | 否 |
| `tracking_relative_body_pos` | 1.0 | gear_sonic | 间接：body FK 受 6 关节物理钉死影响（默认值），不需在 reward 端 mask |
| `tracking_relative_body_ori` | 1.0 | gear_sonic | 同上 |
| `tracking_body_linvel` | 1.0 | gear_sonic | 同上 |
| `tracking_body_angvel` | 1.0 | gear_sonic | 同上 |
| **`action_rate_l2`** | -0.1 | `isaaclab.envs.mdp.action_rate_l2`（stock）→ `sum((a - prev_a)^2)` over **所有 29 dim** | **是** — actor 头 6 维有 finetune 之初的少量噪声会被罚 |
| **`joint_limit`** | -10.0 | `isaaclab.envs.mdp.joint_pos_limits`（stock，`joint_names: [".*"]`） | **是** — 物理钉死值如果在 default 内则不会触发，但保险起见 mask |
| `undesired_contacts` | -0.1 | gear_sonic | 否 |
| `anti_shake_ang_vel` | -0.005 | gear_sonic | 否（基于 wrist body angvel，物理钉死后是 default） |
| `tracking_vr_5point_local` | 2.0 | gear_sonic | 否（5 个关键 link） |
| **`feet_acc`** | -2.5e-7 | `isaaclab.envs.mdp.joint_acc_l2`（stock，`joint_names: [".*ankle.*"]`） | 否（只对 ankle joint，与 missing 无交集） |

**核心需要 mask 的两项**：`action_rate_l2`、`joint_limit`。

**关键源代码**（IsaacLab stock）：

```python
def action_rate_l2(env):
    return torch.sum(torch.square(env.action_manager.action - env.action_manager.prev_action), dim=1)

def joint_pos_limits(env, asset_cfg):
    asset = env.scene[asset_cfg.name]
    out_of_limits = -(asset.data.joint_pos[:, asset_cfg.joint_ids] - asset.data.soft_joint_pos_limits[:, asset_cfg.joint_ids, 0]).clip(max=0.0)
    out_of_limits += (asset.data.joint_pos[:, asset_cfg.joint_ids] - asset.data.soft_joint_pos_limits[:, asset_cfg.joint_ids, 1]).clip(min=0.0)
    return torch.sum(out_of_limits, dim=1)
```

**Mask 插入点**（Phase 3 Task 3.3）：
- 新增 `gear_sonic/envs/manager_env/mdp/rewards.py::action_rate_l2_masked(env)`：在 `torch.square(diff)` 之后、`sum` 之前把 `MISSING_23DOF_INDICES_IL` 列设 0
- 新增 `joint_pos_limits_masked(env, asset_cfg)`：直接通过 `joint_names` 列表把 6 个 missing 排除（用 SceneEntityCfg 的 regex 排除即可，无需复制函数）。最简：在 wrapper YAML 中把 `joint_limit.params.asset_cfg.joint_names` 从 `[".*"]` 替换为剔除 6 个 missing 的列表
- 23dof wrapper YAML override 上述两项

### 13.4 IsaacLab 关节顺序（Task 1.4）

无需运行时 probe — 项目已在 `gear_sonic/envs/env_utils/joint_utils.py:10` 提供 `G1_ISAACLab_ORDER`（29 个名字），权威来源。基于该列表计算的 missing 6 个关节的 IL idx：

| MJCF idx | Joint name | IL idx |
|---|---|---|
| 13 | waist_roll_joint | 5 |
| 14 | waist_pitch_joint | 8 |
| 20 | left_wrist_pitch_joint | 25 |
| 21 | left_wrist_yaw_joint | 27 |
| 27 | right_wrist_pitch_joint | 26 |
| 28 | right_wrist_yaw_joint | 28 |

→ `MISSING_23DOF_INDICES_IL = [5, 8, 25, 26, 27, 28]`（已经在 Task 2.1 中以 provisional 写入 `joint_constants.py`）

→ `joint_constants.py:MISSING_23DOF_INDICES_IL` 注释中"Until Task 1.4 confirms"caveat 现在可以删除。

---

## 14. Motion pool size 与 GPU 显存预算（2026-05-25 补记）

> 上下文：H5h 周末 60k 长跑后做 sim2sim 部署对照,出现"原作者 ckpt 稳 / H5h 各 iter 全摔"的反直觉退化。Test 2 forward-pass 测得 μ 偏离单调上升(iter200=0.96 / iter1k=1.78 / iter60k=4.87)。调查"是不是只见过一小部分数据训练"时挖出本节内容,见 `D1_23DOF_DEBUG_LOG.md` 2026-05-25 H5i 节。

### 14.1 两道 cap

motion pool 实际生效大小 = `min(num_envs, 1024)`,由两处共同约束:

**Cap 1(启动 load)— `gear_sonic/envs/manager_env/mdp/commands.py:238`:**

```python
if max_num_load_motions is None:
    if self.cfg.use_paired_motions:
        self.max_num_load_motions = self.motion_lib._num_unique_motions
    else:
        self.max_num_load_motions = min(self.num_envs, 1024)  # ← 1024 hard cap
else:
    self.max_num_load_motions = max_num_load_motions
self.motion_lib.load_motions_for_training(max_num_seqs=self.max_num_load_motions)
```

**Cap 2(resample)— `gear_sonic/envs/wrapper/manager_env_wrapper.py:1090`:**

```python
def resample_motion(self):
    res = self._motion_lib.load_motions_for_training(
        max_num_seqs=min(self.num_envs, self.motion_command.max_num_load_motions)
    )
```

启动只走 Cap 1,resample 走 Cap 2。Cap 2 又用 `num_envs` 卡一次 → 即使启动塞 `max_num_load_motions=1024`,第一次 resample 后 pool 缩回 `num_envs`。**净效果天花板 = `min(num_envs, 1024)`,1024 是绝对上限。**

### 14.2 原作者训练强度 vs. 本地强度

| 配置 | num_envs | pool size | 4096 motions / 129785 dataset 覆盖 |
|------|----------|-----------|-------------------------------------|
| sonic_release.yaml(原作者 default) | 4096 | **1024**(被 Cap 1 钉死) | 0.79% per pool |
| 本地 D1 训练(8GB 4060) | 512 | 512 | 0.39% per pool |

原作者设计强度是 **1024 motions × 4096 envs(每 motion 平均 4 个 env 同时跑)**。本地 8GB 卡只能 512 envs,等同**只用了原作者一半的 motion 多样性**。

`im_resample.motion_resample_frequency=250`(callbacks/im_resample.yaml)的设计参数也是配 1024 pool 校准过的:每 pool 走 250 PPO step ≈ 250×4096 = 1M sample,在 1024 motions 上重复 ~1000 次 / motion。本地 512 pool 等量 PPO 强度等于每 motion 重复 ~500 次,**密度反而更高**(num_envs 减半压低了重复度)——但 motion **多样性**下降一半,这个 tradeoff 不是对称的。

### 14.3 GPU 显存预算估算

| 组件 | 显存(测得 / 估) |
|------|-------------------|
| GPU 总量(4060) | 8188 MiB |
| Idle baseline | 561 MiB |
| 512 motions FK 解码后(8 个张量:body_pos_w / body_quat_w / body_pos_b / 三速度场 / dof_pos / dof_vel) | ~430 MB(线性外推) |
| 1024 motions(2×) | ~860 MB |
| Isaac sim + PPO buffer + actor/critic | ~6-7 GB(剩余) |

外推:全量 129,785 motions FK 解码后 ≈ 109 GB,**根本装不下任何单卡**。这是为什么 motion 数据**必须分批 resample**——不是带宽问题,是显存问题。

### 14.4 解锁路径(若想本地把 pool 拉到 1024)

需要**同时改两处**,否则只第一次 load 1024,resample 后回落:

1. `commands.py:238`:把 `min(self.num_envs, 1024)` 改为 `1024`,或允许 cmdline `++max_num_load_motions=1024`
2. `manager_env_wrapper.py:1090`:去掉 `min(self.num_envs, ...)`,直接 `max_num_seqs=self.motion_command.max_num_load_motions`

显存可行性:512→1024 多 ~430 MB GPU,4060 8GB 上理论可行(剩余 6 GB 给 sim+PPO),需实测。

### 14.5 决策记录

H5i(2026-05-25 launched)只动 reload 频率(`motion_resample_frequency 250→50`),**不动 pool size**。理由:

- **单维干预原则**:H5i 同时改 freq + pool 出问题不知主因。先看 freq=50 能否把 H5h 的 μ 漂移压回去
- **效果对称性**:freq 250→50 等价 5× 加速 motion 轮换,iter 1000 时 unique 见过数从 H5h 的 ~2k 涨到 ~10k(覆盖 1.6%→7.7%);pool 512→1024 是 2× 单 pool 多样性。两者**叠加效果**预期 10×,但 H5i freq 已经 5× 收益,边际是否划算待数据
- **保留 H5j 卡位**:若 H5i iter 1000 μ 偏离从 H5h 的 1.78 仅降到 1.5(说明 freq 单维干预不足),开 H5j 试 pool=1024

### 14.6 经验

- **GPU 显存才是 motion pool size 的硬约束,32GB RAM 不是。** 全量 dataset 解码后 ≈ 109 GB,远超任何单卡。motion 必须按 pool 流式加载是结构性设计,不是 bug
- **原作者的 hyperparam 是配 1024 pool 校准过的。** 把 num_envs 砍到 512 时,`motion_resample_frequency: 250` 这个值的语义已经不一样了——单 pool 内 PPO 重复曝光的密度变了。本地训练改 num_envs 时必须**同步重审**所有跟 batch size 相关的 callback 配置
- **两个 cap 同时存在时,任何一个 cap 单独移除都没用。** 看到 cmdline `+max_num_load_motions=1024` 能 pass,先验证 resample 路径有没有也读这个值;否则只有 startup 那一次拿到 1024,稳态下就被 Cap 2 削回去,debug 会非常迷

